[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "logseq-analyzer"
authors = [{name = "Wonyoung", email = "contact@wonyoungjang.org"}]
dynamic = ["version", "description"]